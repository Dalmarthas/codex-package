interface:
  display_name: "Swift Concurrency 6 2"
  short_description: "Swift 6.2 Approachable Concurrency — single-threaded by default, @concurrent for explicit background offloading, isolated conformances for m"
  brand_color: "#3B82F6"
  default_prompt: "Apply the Swift Concurrency 6 2 skill workflow from SKILL.md to this task."
policy:
  allow_implicit_invocation: true
