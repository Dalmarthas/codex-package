interface:
  display_name: "Jpa Patterns"
  short_description: "JPA/Hibernate patterns for entity design, relationships, query optimization, transactions, auditing, indexing, pagination, and pooling in Sp"
  brand_color: "#3B82F6"
  default_prompt: "Apply the Jpa Patterns skill workflow from SKILL.md to this task."
policy:
  allow_implicit_invocation: true
