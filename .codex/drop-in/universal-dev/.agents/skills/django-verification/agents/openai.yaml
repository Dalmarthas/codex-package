interface:
  display_name: "Django Verification"
  short_description: "Verification loop for Django projects: migrations, linting, tests with coverage, security scans, and deployment readiness checks before rele"
  brand_color: "#3B82F6"
  default_prompt: "Apply the Django Verification skill workflow from SKILL.md to this task."
policy:
  allow_implicit_invocation: true
